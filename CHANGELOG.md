# Changelog

### v1.4.0 - 2026-09-15

**📊 用量统计校准 + 多窗口重复计数修复**

* 新增用量统计校准：每条消息自动对照框架记录的当日真实用量（与 WebUI 面板同源），把带工具调用的多步对话、请求中断、模型耗尽后等插件看不到的消耗补回限额判定，切换时机与面板/后端一致。查询失败自动按原计数处理，无需配置。
* 修复多窗口引用同一模型时，aiimg 补拍、grok 搜索等插件上报的用量被重复计入每个窗口（放大数倍）的问题，现归属到第一个引用该模型的窗口。

---

### v1.3.6

**🔗 存图路由共享聊天日额度**

* 存图模型与聊天模型相同时，两者消耗合并计算日额度：任一方达到限额都会触发存图侧切换，聊天侧顺延逻辑不变。
* 新增 `get_provider_daily_usage(provider_id)` 接口，返回某 provider 当日总用量。
* 旧版调用方自动回退为独立额度，不受影响。

---

### v1.3.5

**🔌 插件用量并入聊天每日额度**

* 新增 `record_plugin_usage` 接口，供 aiimg 补拍、grok 搜索等插件上报 token 消耗。
* 插件消耗计入与聊天相同的每日额度：插件与聊天合计达到某模型限额时，聊天照常顺延到下一模型；插件自身不参与路由。

---

### v1.3.4

**🔌 存图模型用量追踪接口**

* 新增 `get_active_storage_provider` / `record_storage_usage` / `get_storage_usage` 方法，供 wardrobe 等插件实现存图模型按日用量路由。
* 支持任意数量模型的回退链：按顺序找第一个未达限额的，全部达限额返回最后一个。
* 存图用量按 provider 独立追踪，每日 0 点重置。

---

### v1.3.3

**🎛️ /路由 命令**

* 新增 `/路由` 命令，切换当前窗口(UMO+人格)的路由启用状态。
* 仅对已显式配置该人格的窗口生效；关闭后插件暂停介入，使用 astrbot 默认链路，再次发送可恢复。
* 禁用状态持久化，重启不丢失，不随每日重置改变。

---

### v1.3.2

**🐛 群聊读空气场景路由修复**

* 修复 chatplus 等插件的"读空气"在非@消息上触发 LLM 调用时路由不生效、导致使用默认模型（可能已暂停）的问题。
* 改为按 UMO 快速过滤：消息属于已配置窗口即预设模型，指令消息不受影响。

---

### v1.3.1

**🔍 调试模式增强**

* debug 开启后，消息处理与响应记录的所有跳过点均输出原因（未匹配窗口、人格解析结果、模型不在路由链中等），便于定位群聊不生效等问题。

---

### v1.3.0

**🎛️ 新增调试模式**

* 新增 `debug` 配置项：开启后输出本次使用模型、用量累加(before→after)、模型切换(A→B) 全链路日志。

---

### v1.2.0

**🎛️ 人格选择式配置 + 窗口扩展到 10 个**

* 窗口的人格字段改为下拉选择，选项由框架实时提供，新增/删除人格自动同步；留空仍表示对所有人格生效。
* 窗口槽位从 5 个扩展到 10 个。

---

### v1.1.0

**🎭 基于人格的独立路由**

* 每个窗口可绑定一个人格，实现同一窗口内多人格各自独立的路由链、用量计数与「已用尽」标记。
* 人格留空时对所有人格生效（兼容旧版），匹配优先级：指定人格 > 通用窗口。
* 旧版用量数据自动迁移到新格式，计数不丢失。

---

### v1.0.0

**🔀 首次发布**

* 按对话窗口(UMO)追踪 token 用量，模型达到每日限额后自动切换到路由链中的下一个模型，全部用尽后回退框架默认模型。
* 支持 window（每窗口独立计数）/ global（所有窗口共享额度）两种统计模式。
* 每天 0 点自动重置用量与「已用尽」标记，用量数据持久化，重启不丢失当日计数。
* 全程静默运行，不提供命令、不发提醒。
